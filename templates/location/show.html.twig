{% extends "location/base.html.twig" %}

{% block body %}
    <h1>Location</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ location.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ location.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('location_edit', location.rp) }}">edit</a>
    {{ include('location/_delete_form.html.twig') }}
{% endblock %}
